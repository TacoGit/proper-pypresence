from setuptools import setup, find_packages

setup(
    name='pypresence',
    version='3.5.1',
    packages=find_packages(),
    install_requires=[
        'requests',  # Add any other dependencies the module needs
    ],
)